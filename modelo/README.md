# Model files

Place the trained emotion model in this folder.

Expected file:

emociones_modelo.joblib

Calibration files may be generated during execution, but they are ignored by Git because they may contain personal runtime data.
